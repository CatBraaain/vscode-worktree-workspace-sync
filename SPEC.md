# Worktree Workspace Sync 仕様


## 対象と適用範囲

- 起動: エディタ起動完了時に起動する。ワークスペースフォルダを1つ以上開いたウィンドウで動く。
- 停止: 拡張のアンロード時にはポーリングを停止する。
- 主リポジトリ: 起動時のワークスペースフォルダ0。本拡張はこれを変更しない。

## 起動時のワークスペース遷移

- 同期が有効 (`enabled` が `true`) で、ワークスペースを保存済みファイルで開いていない、かつワークスペースフォルダが1つのとき、起動時に untitled ワークスペースへ遷移する。これにより、以降のワークツリー追加時の一括再描画は発生しない。
- 遷移は次の準備が揃った時点で行う。準備待ちの合計が上限 (3秒) に達したときは待ちを打ち切り、その時点で遷移する。準備待ちの間、同期 (ポーリング) は開始しない。
  - GitLens 拡張がインストールされている場合、その起動が完了していること。インストールされていない場合はこの待ちを行わない。
  - VS Code 内蔵の Git 機能がワークスペースフォルダ0に対応するリポジトリを認識していること。対応とは、リポジトリのルートがフォルダ0と一致するか、いずれかが他方の配下にあること。認識していない場合は認識するまで待つ。
  - GitLens 拡張の起動に失敗した場合も待ちを続け、残る準備が揃えば遷移する。
- 遷移前後でワークスペースフォルダ0のパス・順序・フォルダ名は変わらない。
- 遷移により、起動時に一度だけ再描画が発生する。
- 保存済みワークスペースファイルで開いている場合と、ワークスペースフォルダが2つ以上の場合は遷移しない。
- 遷移に失敗したときは、何もせず通知もしない。同期は次のポーリングで通常どおり動き、追加の適用時に遷移する。

## 設定

`worktreeWorkspaceSync` セクション:

| キー | 型 | デフォルト | 振る舞い |
|---|---|---|---|
| `enabled` | boolean | `true` | オンの場合のみ同期が動く |
| `pollIntervalMs` | number | `2500` | ポーリング間隔 (ms)。指定値と500の大きい方を採用する |
| `roots` | string[] | `[]` | 空なら主リポジトリの全ワークツリーを対象にする。非空なら指定パス (主リポジトリ相対または絶対) の厳密配下のみを対象にする |

設定変更はウィンドウ再読み込みなしで次のポーリングから反映される。

## 同期の定義

- `pollIntervalMs` 間隔で主リポジトリのワークツリー一覧 (`git worktree list --porcelain`) を読む。一覧は常に新規に読む。
- 同期対象: 主リポジトリの生存ワークツリーのうち、ベアでなく主リポジトリ自身でないもの (`roots` で絞り込み)。
- 同期先: エクスプローラーのワークスペースフォルダ一覧と、ソースコントロール。ソースコントロールへの反映は、ワークスペースフォルダの出入りに対して VS Code 内蔵の Git がリポジトリを開くことで行われ、本拡張が担うのはワークスペースフォルダの出入りまでである。

| ワークツリーの変化 | エクスプローラー | ソースコントロール |
|---|---|---|
| 存在し、ワークスペースにまだ無い | フォルダとして現れる。名前は下記のフォルダ名 | リポジトリとして現れ、diff・commitができる |
| 消える、または同期対象でなくなる | フォルダから消える。開いていたエディタタブは自動で閉じる | リポジトリも消える |
| 同期対象外のフォルダ (ユーザーが追加したもの、主リポジトリ自身) | 変更しない | — |

- フォルダ名: ブランチ名。ブランチを持たないときは `(detached <HEADの先頭7文字>)`。パスが `.claude/worktrees` 配下のときは末尾に `  ·  agent` を付ける。
- 出現は作成から概ね `pollIntervalMs` 以内で、ユーザー操作を要しない。
- 反映にウィンドウ再読み込みを要しない。
- ポーリングでの git の読み取りやワークスペースへの適用に失敗したときは、同期状態を変えずに次のポーリングで再試行する。通知と同期の停止は行わない。

## ソースコントロール表示の修復

- ポーリングごとに、同期対象のワークツリーのフォルダがワークスペースに存在するのに、VS Code 内蔵の Git が開いたリポジトリのルートがそのフォルダのパスと一致しない状態 (欠落) を検出する。判定対象はワークスペースフォルダのうち、フォルダ0と同期対象でないフォルダを除いたもので、ルートの一致はパスの完全一致で判定する。
- 欠落を初めて検出してから 15 秒が経過したポーリングで、そのフォルダをワークスペースから一度取り除いてから再追加し、リポジトリの再検出を促す。1回のポーリングで修復するフォルダは1つ。
- 修復後も欠落が続くときは、再び 15 秒の猶予ののち修復を繰り返す。修復回数の上限はない。
- 修復の取り除きと再追加は分けて行い、取り除きの反映を待ってから再追加する。再追加したフォルダの位置はワークスペースフォルダ一覧の末尾、フォルダ名は「同期の定義」のフォルダ名と同じになる。
- 修復の取り除きで、該当フォルダで開いていたエディタタブは閉じる。再追加後にタブを開き直さない。
- 内蔵の Git 機能の API が取得できない場合 (拡張が無効、または起動に失敗) は、修復を行わない。
- 修復では通知を行わない。

## タイトルバー

- ワークスペースを保存済みファイルで開いている場合と、単一フォルダとして開いている場合を除き (untitled ワークスペース)、タイトルバーのテンプレート (`window.title` の既定値) は VS Code では次になっている。

  ```text
  ${dirty}${activeEditorShort}${separator}${rootName}${separator}${profileName}${separator}${appName}
  ```

  本拡張はこれを次に変更する。`${rootName}` の代わりに主リポジトリのフォルダ名を埋め込むもので、他の変数はそのまま残る。

  ```text
  ${dirty}${activeEditorShort}${separator}<主リポジトリのフォルダ名>${separator}${profileName}${separator}${appName}
  ```

- 例: アクティブエディタで `title.ts` を開いているとき、既定では `title.ts - Untitled (Workspace) - Visual Studio Code` と表示されるところを、`title.ts - vscode-worktree-workspace-sync - Visual Studio Code` と表示する。
- 変更は `window.title` 設定へのワークスペーススコープの書き込みによる。反映にウィンドウ再読み込みを要しない。
- 書き込み先は untitled ワークスペースのワークスペース設定であり、リポジトリ内にファイルを作成・変更しない。
- 単一フォルダとして開いている間は書き込まない。起動時のワークスペース遷移により untitled ワークスペースになった後のセッションで書き込む。
- 保存済みワークスペースファイルで開いている場合は書き込まない。
- 書き込みに失敗したときは、何もせず通知もしない。
