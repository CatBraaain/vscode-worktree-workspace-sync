# Worktree Autosync 仕様


## 対象と適用範囲

- 起動: エディタ起動完了時に起動する。ワークスペースフォルダを1つ以上開いたウィンドウで動く。
- 主リポジトリ: 起動時のワークスペースフォルダ0。本拡張はこれを変更しない。

## 設定

`worktreeAutoSync` セクション:

| キー | 型 | デフォルト | 振る舞い |
|---|---|---|---|
| `enabled` | boolean | `true` | オンの場合のみ同期が動く |
| `pollIntervalMs` | number | `2500` | ポーリング間隔 (ms)。指定値と500の大きい方を採用する |
| `roots` | string[] | `[]` | 空なら主リポジトリの全ワークツリーを対象にする。非空なら指定パス (主リポジトリ相対または絶対) の厳密配下のみを対象にする |

設定変更はウィンドウ再読み込みなしで次のポーリングから反映される。

## 同期の定義

- `pollIntervalMs` 間隔で主リポジトリのワークツリー一覧 (`git worktree list --porcelain`) を読む。一覧は常に新規に読む。
- 同期対象: 主リポジトリの生存ワークツリーのうち、ベアでなく主リポジトリ自身でないもの (`roots` で絞り込み)。
- 同期先: エクスプローラーのワークスペースフォルダ一覧と、ソースコントロール。

| ワークツリーの変化 | エクスプローラー | ソースコントロール |
|---|---|---|
| 存在し、ワークスペースにまだ無い | フォルダとして現れる。名前は下記のフォルダ名 | リポジトリとして現れ、diff・commitができる |
| 消える、または同期対象でなくなる | フォルダから消える。開いていたエディタタブは自動で閉じる | リポジトリも消える |
| 同期対象外のフォルダ (ユーザーが追加したもの、主リポジトリ自身) | 変更しない | — |

- フォルダ名: ブランチ名。ブランチを持たないときは `(detached <HEADの先頭7文字>)`。パスが `.claude/worktrees` 配下のときは末尾に `  ·  agent` を付ける。
- 出現は作成から概ね `pollIntervalMs` 以内で、ユーザー操作を要しない。
- 反映にウィンドウ再読み込みを要しない。

## タイトルバー

- ワークスペースを保存済みファイルで開いていない (untitled) 場合、タイトルバーは "Untitled (Workspace)" の代わりに主リポジトリのフォルダ名を表示する。
- 実現は `window.title` 設定へのワークスペーススコープの書き込みによる。反映にウィンドウ再読み込みを要しない。
- 保存済みワークスペースファイルで開いている場合は書き込まない。
